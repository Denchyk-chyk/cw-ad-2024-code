<Window x:Class="CS.Test.Ui.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fields="clr-namespace:CS.General.Form.Field"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen" Style="{StaticResource FormWindow}">
    <Grid Style="{StaticResource FormGrid}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="53*"/>
            <ColumnDefinition Width="172*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <fields:TextField Title="TextField" Grid.Row="0" Grid.ColumnSpan="2" Margin="20,10,20,10"/>
        <fields:DateField Title="DateField" Grid.Row="1" Grid.ColumnSpan="2" Margin="20,10,20,10"/>
        <fields:ListField Title="ListField" Grid.Row="2" Grid.ColumnSpan="2" Margin="20,10,20,10"/>
    </Grid>
</Window>